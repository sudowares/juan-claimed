node_modules
dist
.env
.env.*
.vercel
.claude/settings.local.json

# Generated by the frontend container on start (dev) / by Dockerfile.prod's build (prod) —
# both copy the repo's own docs/ folder fresh each time, so this is never hand-maintained
# and never committed. See docker-compose.yml's frontend service + frontend/Dockerfile.
frontend/public/docs/
